{
  "name": "hello",
  "version": "0.0.0",
  "description": "hello",
  "config": {
    "phone": "192.169.1.107"
  },
  "scripts": {
    "build": "gulp ls-compile&&pebble build",
    "debug": "npm run build&&pebble install --emulator aplite&& pebble logs",
    "debug-basalt": "npm run build&&pebble install --emulator basalt&& pebble logs",
    "debug-chalk": "npm run build&&pebble install --emulator chalk&& pebble logs",
    "debug-phone": "npm run build&&pebble install --phone $PHONE --logs",
    "server": "lsc app.ls"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pebble/pebblejs.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/pebble/pebblejs/issues"
  },
  "homepage": "https://github.com/pebble/pebblejs#readme",
  "devDependencies": {
    "express": "^4.13.4",
    "gulp": "^3.9.0",
    "gulp-clean": "^0.3.2",
    "gulp-livescript": "^3.0.0",
    "livescript": "^1.4.0"
  }
}
